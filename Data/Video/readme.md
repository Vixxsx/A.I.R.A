# Video Storage

This folder contains interview videos and extracted frames.

## Structure
- `raw/` - Original recordings (NOT in Git)
- `processed/` - Processed videos (NOT in Git)
- `frames/` - Extracted frames for AI analysis (NOT in Git)

## Important
Video files are NOT tracked by Git due to size.
Only folder structure (.gitkeep files) is committed.